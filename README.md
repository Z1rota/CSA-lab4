
### Отчет по Лабораторной работе №4


- ФИО: Смирнов Андрей Алексеевич
- Группа: P3213
- Вариант: `asm | stack | neum | hw | tick | binary | trap | port | pstr | prob1 | superscalar`

## Язык программирования Asm (Stack)

Синтаксис языка представляет собой ассемблер, адаптированный под стековую архитектуру. Код разделяется на секции данных и текста.

Форма Бэкуса-Наура (БНФ):
```
<program> ::= <section_data> <section_text>
<section_data> ::= ".data\n" <data_decl>*
<data_decl> ::= <label> ":" <directive> <value> "\n" | <org_directive>
<directive> ::= ".word" | ".pstr"
<section_text> ::= ".text\n" <code_line>*
<code_line> ::= [<label> ":"] <instruction> [<operand>] | <macro_def> | <org_directive>
<macro_def> ::= "%define" <name> <value>
<org_directive> ::= ".org" <address>
```


**Особенности семантики:**
- Поддерживаются макросы `%define` для констант.
- Строки хранятся в формате Pascal strings (`.pstr`): первое слово — длина строки, последующие слова — символы (по одному символу в 32-битном слове).
- Архитектура стековая: математические операции операндов не принимают, а берут значения с вершины стека (TOS) и кладут результат обратно.
- Директива `.org` позволяет размещать код и данные по абсолютным адресам.

## Организация памяти и аппаратный стек

- **Архитектура:** Фон-Неймана.
- **Машинное слово:** 32 бита, знаковое целое.
- Процессор содержит два независимых аппаратных стека:
  - `Data Stack` (Стек данных) — используется исключительно для вычислений АЛУ. Доступ к нему идет через вершину стека (TOS). (Глубина 256).
  - `Return Stack` (Стек возвратов) — аппаратно используется для сохранения PC при вызове процедур (`CALL`) и прерываниях. (Глубина 256).
- **Прерывания:** Вектор прерываний жёстко задан по адресу `0x0010`.
- 
## Система команд

Инструкции упаковываются в одно 32-битное машинное слово:
- Старшие 8 бит: `Opcode` (код операции).
- Младшие 24 бита: Операнд (знаковое число).

### Набор инструкций

Для описания семантики команд используются следующие обозначения:
- `DS` (Data Stack) — аппаратный стек данных (используется для АЛУ).
- `RS` (Return Stack) — аппаратный стек возвратов (для `CALL`, `RET` и прерываний).
- `MEM` — общая память (массив машинных слов).
- `PC` — счётчик команд (Program Counter).
- `PORT` — адресное пространство портов ввода-вывода.
- `IE` — флаг разрешения прерываний (Interrupt Enable).

| Мнемоника | Опкод | Действие (Семантика) | Операнд (`arg`) |
|-----------|-------|----------------------|-----------------|
| `NOP`     | 0x00  | Нет операции | Игнорируется |
| `PUSH`    | 0x01  | `DS.push(arg)` | Число (константа) |
| `PUSH_M`  | 0x02  | `DS.push(MEM[arg])` | Адрес памяти |
| `POP_M`   | 0x03  | `MEM[arg] <- DS.pop()` | Адрес памяти |
| `ADD`     | 0x04  | `b = DS.pop(); a = DS.pop(); DS.push(a + b)` | Игнорируется |
| `SUB`     | 0x05  | `b = DS.pop(); a = DS.pop(); DS.push(a - b)` | Игнорируется |
| `MOD`     | 0x06  | `b = DS.pop(); a = DS.pop(); DS.push(a % b)` | Игнорируется |
| `CMP`     | 0x07  | `b = DS.pop(); a = DS.pop(); DS.push(a == b ? 1 : 0)` | Игнорируется |
| `GT`      | 0x13  | `b = DS.pop(); a = DS.pop(); DS.push(a > b ? 1 : 0)` | Игнорируется |
| `MUL`     | 0x11  | `b = DS.pop(); a = DS.pop(); DS.push(a * b)` | Игнорируется |
| `DIV`     | 0x12  | `b = DS.pop(); a = DS.pop(); DS.push(a / b)` | Игнорируется |
| `JMP`     | 0x08  | `PC = arg` | Адрес перехода |
| `JZ`      | 0x09  | `if (DS.pop() == 0) { PC = arg }` | Адрес перехода |
| `CALL`    | 0x0A  | `RS.push(PC); PC = arg` | Адрес процедуры |
| `RET`     | 0x0B  | `PC = RS.pop()` | Игнорируется |
| `IN`      | 0x0C  | `DS.push(PORT[arg])` | Номер порта |
| `OUT`     | 0x0D  | `PORT[arg] <- DS.pop()` | Номер порта |
| `IRET`    | 0x0E  | `PC = RS.pop(); IE = 1` | Игнорируется |
| `EI`      | 0x10  | `IE = 1` | Игнорируется |
| `HALT`    | 0x0F  | Остановка тактового генератора | Игнорируется |
| `LOAD`    | 0x14  | `addr = DS.pop(); DS.push(MEM[addr])` | Игнорируется |
| `STORE`   | 0x15  | `val = DS.pop(); addr = DS.pop(); MEM[addr] <- val` | Игнорируется |

### Ввод-вывод 
Используется система прерываний с аппаратным таймером (расписанием):
- При наступлении заданного такта, внешний контроллер кладёт символ в буфер и дёргает линию прерывания.
- Процессор аппаратно сохраняет `PC` на `Return Stack`, сбрасывает флаг `IE` (Interrupt Enable) и переходит по вектору `0x0010`.
- Ввод/вывод маппится на порты:
  - Порт `0`: Чтение символа.
  - Порт `1`: Символьный вывод.
  - Порт `2`: Числовой вывод.

## Микроархитектура, Такты и Суперскалярность

В отличии от базовой реализации, процессор моделирует многотактовое выполнение:
1. **Fetch (Выборка):** Занимает 1 такт. Шина памяти позволяет выбирать сразу 64 бита (2 инструкции) за такт.
2. **Execute (Выполнение):** Зависит от инструкции. Арифметические операции и переходы занимают 1 такт. Инструкции работы с ОЗУ (`PUSH_M`, `POP_M`, `LOAD`, `STORE`) занимают 2 такта.

В классической стековой архитектуре все операции проходят через вершину стека (TOS), что вызывает постоянные структурные конфликты. Для реализации **суперскалярного выполнения** без блокировок АЛУ применяется паттерн аппаратного **теневого регистра (TOS_SHADOW)** для отложенной фоновой записи в память.

Control Unit (CU) за 1 такт Fetch достает 2 инструкции. Если первая из них использует АЛУ (например, `SUB`, `IN`), а вторая — сохраняет данные в память (`POP_M`), CU запускает их параллельно:
1. Результат вычисления АЛУ мгновенно помещается в `TOS_SHADOW`. Стек освобождается.
2. Фоновая запись из `TOS_SHADOW` в ОЗУ занимает следующие 2 такта, параллельно с этим процессор продолжает выборку и работу со следующими переменными.
3. Если происходит чтение (`PUSH_M`) переменной, которая еще пишется в память, происходит аппаратный **Bypass** напрямую из теневого регистра (0 тактов ожидания памяти).

### Сравнение производительности

*В таблице приведены точные метрики, полученные при прогоне симулятора с флагом `--no-superscalar` и без него.*

| Алгоритм   | Без суперскалярности (Такты) | С суперскалярностью (Такты) | Прирост скорости |
|------------|------------------------------|-----------------------------|------------------|
| cat        | 70                           | 62                          | 13%              |
| hello_user | 1594                         | 1414                        | 12%              |
| sort       | 1387                         | 1227                        | 13%              |
| prob1      | 1986271                      | 1671109                     | 18%              |

Как видно из результатов, объединение вычислений АЛУ с фоновой записью в память и аппаратный Bypass из теневого регистра позволили значительно сократить количество тактов простоя конвейера, обеспечив существенный прирост производительности.
## Транслятор

Интерфейс командной строки: `python translator.py <source.asm> <output.bin>`

Транслятор реализован в два прохода:
1. **Pass 1:** Удаление комментариев, резолвинг макросов `%define`, вычисление адресов меток, генерация данных (`.word`, `.pstr`) в память с учётом директив `.org`.
2. **Pass 2:** Подстановка адресов вместо меток, упаковка инструкций (Opcode + Arg) в 32-битные бинарные слова. Вывод в бинарный файл (`.bin`) и человекочитаемый лог (`.bin.log`).

## Модель процессора

Интерфейс командной строки: `python machine.py <code.bin> <schedule.txt>`

Архитектура разделена на **Control Unit (Диспетчер)** и **Data Path (Тракт данных)**. Процессор поддерживает потактовое исполнение (tick-by-tick) и аппаратную суперскалярность.

### Схема процессора (с теневым регистром)
```
       Interrupt Timer (Schedule)
             |
             v
      +---------------+         [Port 0 IN] --->+
      | Interrupt     |---> IE                  |
      | Controller    |---> Trap Vector (0x10)  |
      +---------------+                         v
                                         +-------------+
            +--------------------------->| ALU         |
            |            +----+          | Execution   |---> [Port 1 Char OUT]
            |      +---->| TOS|<-------->| Unit 1      |
            |      |     +----+          +-------------+---> [Port 2 Int OUT]
            |      |       ^                    |
            |      |   (Hardware Data Stack)    |
            |      |     (Depth: 256)           |
            v      v                            | (1 такт: Bypass / Swap)
    +--------------------+                      v
    |   Main Memory      |               +-------------+
+-->|   (Code + Data)    | <=============| TOS_SHADOW  |
|   |   (Von Neumann)    |   (Фоновая    | Register    |
|   +--------------------+  запись: 2 т.)+-------------+
|            |      |
|  (Fetch 64-bit: 2 Instructions)
|            |      |
|            v      v
|     +--------------------+
|     | Instruction Queue  |
|     +--------------------+
|          |         |
|      Inst 1       Inst 2
|          |         |
|          v         v
|     +--------------------+
|     | Control Unit       |
|     +--------------------+
|               | (Jumps / Traps)
|               v
|     +--------------------+       +----------------+
|     | Control Execution  |       | Hardware       |
|     | Unit 2             | <---> | Return Stack   |
|     +--------------------+       | (Depth: 256)   |
|        |                         +----------------+
|        v      
|      +----+   
+------| PC |   
       +----+
```
## Тестирование

Реализованные алгоритмы:
1. `cat.asm` — Печать данных, подаваемых на ввод (до символа EOF).
2. `hello_user_name.asm` — Запрос имени пользователя, сохранение в буфер памяти и вывод приветствия.
3. `sort.asm` — Пузырьковая сортировка массива через косвенную адресацию (`LOAD` / `STORE`).
4. `double_math.asm` — Демонстрация 64-битной математики (сложение двух чисел по 32 бита с учетом переноса разряда).
5. `prob1.asm`  — Поиск максимального палиндрома от умножения двух трехзначных чисел. Вывод через порт 2. 


Запуск автоматических тестов:
```
pytest -v
```

Ручная трансляция и запуск (на примере алгоритма `cat`):
```
> python translator.py examples/cat.asm target.bin
> python machine.py target.bin examples/input_schedule.txt
--- Simulation Started ---
Tick: 0001 | PC: 0000 | Stk: [] | Exec: JMP 256
Tick: 0002 | Pipeline Active
Tick: 0003 | PC: 0100 | Stk: [0] | Exec: IN 0 || POP_M 0 (Superscalar)
Tick: 0004 | PIPELINE STALL
Tick: 0005 | Pipeline Active [MemWrite: 0 <- 0]
...
Total Ticks: 62
Instructions Executed: 28
Output: foo
```